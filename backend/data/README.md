# Data

`raw/` stores generated synthetic source data. `processed/` stores train, validation and test splits.

Expected fields include purchase amount, customer and account identifiers, transaction history signals, treatment (`allow` or `verify`) and fraud outcome.
